# EN: Generated by scripts/build-overlay.mjs -- a fresh snapshot of src/
#     with the 071-now SQLite shim overlaid. Rebuilt on every build.
# JA: scripts/build-overlay.mjs が生成 -- src/ のスナップショットに
#     071-now SQLite シムを重ねたもの。ビルドごとに再生成される。
wp/

# EN: Vite build output and the verification artifacts.
# JA: Vite のビルド成果物と検証成果物。
dist/
node_modules/
test/071-now-frontpage.png
