; EN: Load the PHP 7/8 compatibility shim before every request, so its
;     functions are defined regardless of each entry point's include order.
; JA: PHP 7/8 互換シムを毎リクエストの前に読み込み、各エントリポイントの
;     include 順に依存せず互換関数が定義されるようにする。
auto_prepend_file = /var/www/html/b2-include/php-compat.php
