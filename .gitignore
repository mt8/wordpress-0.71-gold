# Claude Code -- project instructions and local state (agent worktrees etc.)
# / Claude Code -- プロジェクト指示とローカル状態(エージェント worktree 等)
CLAUDE.md
.claude/

# Composer / static analysis / test tooling
/vendor/
.phpcs.cache
.phpunit.cache
.phpunit.result.cache

# Static-export output (generated by `071 export`) / 静的書き出しの出力
/static-export/

# E2E (Playwright) tooling / E2E (Playwright) ツール
node_modules/
test-results/
playwright-report/
/playwright/.cache/

# Block editor (src/block-editor/), Issue #65 / #76 / ブロックエディタ
# EN: npm dependencies and the bundled build output are artifacts, not source.
# JA: npm の依存関係とバンドルされたビルド成果物は成果物でありソースではない。
/src/block-editor/app/node_modules/
/src/block-editor/assets/

# 071-env local config and generated overrides / 071-env のローカル設定と生成物
# EN: `.071-env.override.json` is a per-developer local override, the analogue
#     of wp-env's `.wp-env.override.json` -- it is not committed.
#     `docker-compose.071-mappings.yml` is regenerated by 071-env on every run
#     from the `mappings` field of `.071-env.json`; it is a build artifact.
# JA: `.071-env.override.json` は開発者ごとのローカル上書きで、wp-env の
#     `.wp-env.override.json` の相当物 -- コミットしない。
#     `docker-compose.071-mappings.yml` は 071-env が毎回 `.071-env.json` の
#     `mappings` フィールドから再生成する。ビルド成果物である。
/.071-env.override.json
/docker-compose.071-mappings.yml

# Uploaded media (src/wp-content/uploads/), Issue #93 / アップロード済みメディア
# EN: Files uploaded through the block editor are user content, not source.
#     The wp-content/uploads/YYYY/MM/ tree is created on demand by upload.php.
# JA: ブロックエディタからアップロードされたファイルは利用者コンテンツであり
#     ソースではない。wp-content/uploads/YYYY/MM/ のツリーは upload.php が
#     必要時に作成する。
/src/wp-content/uploads/
