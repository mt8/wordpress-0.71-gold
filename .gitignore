CLAUDE.md

# Composer / static analysis / test tooling
/vendor/
.phpcs.cache
.phpunit.cache
.phpunit.result.cache

# Static-export output (generated by bin/static-export.php) / 静的書き出しの出力
/static-export/

# Block-editor prototype, Issue #65 / ブロックエディタ試作 (Issue #65)
# EN: npm dependencies and the bundled build output are artifacts, not source.
# JA: npm の依存関係とバンドルされたビルド成果物は成果物でありソースではない。
/block-editor-prototype/node_modules/
/src/block-editor-assets/
