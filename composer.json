{
    "name": "mt8/wordpress-0.71-gold",
    "description": "Experimental port of the oldest WordPress (0.71-gold) to PHP 8.3 + MySQL 8.",
    "license": "GPL-2.0-or-later",
    "type": "project",
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.7",
        "phpstan/phpstan": "^2.0",
        "wp-coding-standards/wpcs": "^3.1",
        "phpunit/phpunit": "^12.5",
        "behat/behat": "^3.31"
    },
    "scripts": {
        "phpcs": "phpcs",
        "phpstan": "phpstan analyse --memory-limit=1G",
        "test": "phpunit",
        "behat": "bash tools/cli/tests/run-behat.sh",
        "static-export": "php tools/cli/php/071-cli.php export"
    },
    "scripts-descriptions": {
        "behat": "Run the 071-cli Behat functional test suite (starts a dedicated test database; see tools/cli/README.md).",
        "static-export": "Export the running blog to a static HTML site (alias for `071 export`; see docs/static-export.md)."
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    }
}
